# Spec checklist

**Feature:** [link to spec.md]

Mark after `/nuvyn.specify`. Re-check after `/nuvyn.clarify`.

**Pass** is the text `pass` or `fail` — not a `[ ]` / `[x]` box. `/nuvyn.implement` must not treat this file as an unchecked checklist.

| Item | Pass |
| --- | --- |
| Intent is one paragraph, no package names | |
| At least one actor and one P1 user story with Given / when / then and Independent test | |
| Mobile product table filled (offline, permissions, lock, sensitive data, background, navigation) | |
| MP-NAV names a start route and chrome (NavigationPage / tabs / flyout) | |
| Functional requirements are testable | |
| Out of scope is explicit | |
| Success criteria are user-facing (no `Plugin.Maui.*` / `NV*` names) | |
| At most three `[NEEDS CLARIFICATION]` markers | |
